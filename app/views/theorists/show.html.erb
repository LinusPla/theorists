



<div class="container">
  <div class="row">
    <div class="col-sm">
      <img src="https://i.pravatar.cc/150?img" class="rounded-circle"/>
      <figcaption>Rating: </figcaption>
    </div>
    <div class="col-sm">
       <h2><%= @theorist.stage_name %></h2>
         <p><%= @theorist.main_theory %></p>
    </div>
    <div class="col-sm">
      <div class="wrapper">
</div>
    </div>
  </div>
</div>

<%# Sorry for this, just the syntaxes %>

<%= render "reviews/form", locals: {theorist: @theorist, review: @review} %>


  <% @theorist.reviews.each do |review| %>
        <p><%= review.comment %></p>
       <p><%= review.rating %></p>
       <% end %>
