<div class="profile">
        <img src="https://i.pravatar.cc/150?img" alt="" class="photo">
        <span class="name"><%= @theorist.stage_name %></span>
        <br>
        <span class="details"><%= @theorist.main_theory %></span>
        <br>
        <br>
        <div class="buttons">
            <div class="button booking">
                <br>
                <%= link_to "Book now!", new_theorist_booking_path(@theorist, @booking) %>
            </div>
            <div class="button review">
            </div>
        </div>
    </div>

<%# Sorry for this, just the syntaxes %>

<%= render "reviews/form", locals: {theorist: @theorist, review: @review} %>


  <% @theorist.reviews.each do |review| %>
        <p><%= review.comment %></p>
       <p><%= review.rating %></p>
       <% end %>
